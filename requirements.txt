pymupdf>=1.24        # PDF 渲染(含页面旋转处理)
rapidocr>=3.0        # OCR: PP-OCRv6 ONNX 模型, 输出文字+坐标
onnxruntime>=1.17    # rapidocr 的推理后端(CPU)
python-docx>=1.1     # 生成 .docx
pillow>=10.0         # 读取页图尺寸
PySide6>=6.6         # 图形界面(仅 gui.py 需要; 纯命令行可不装)
